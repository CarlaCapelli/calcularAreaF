function calcularAreaTriangulo(base: number, altura: number) {
  let area: number = (base * altura) / 2;
  console.log("el area es " + area);
}

calcularAreaTriangulo(1, 2);
calcularAreaTriangulo(2, 4);
calcularAreaTriangulo(3, 6);
calcularAreaTriangulo(4, 8);
calcularAreaTriangulo(5, 10);
calcularAreaTriangulo(6, 12);
calcularAreaTriangulo(7, 14);
